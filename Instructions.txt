1. Be in TrustViz folder
2. export GEMINI_API_KEY=""
3. uvicorn trustviz.server.app:app --host 127.0.0.1 --port 8000 --reload
4. If #3 doesn't work, load everything from requirements.tx
source .venv/bin/activate
python -m pip install --upgrade pip setuptools wheel
pip install -r requirements.txt


